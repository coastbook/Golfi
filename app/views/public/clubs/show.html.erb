<div class="container" style="margin-top: 50px;">
  <div class="shadow p-3 mb-5 bg-white rounded">
  <div class="row">
    <div class="col-md-3" style="text-align: left;">
　　　<table class="table">
      <h2 class="p-3 mb-2 bg-dark text-white">クラブ詳細</h2>
    </div>
    <div class="col-md-8", style="margin-top: 50px;">
     </table>
      </div>
    <div class="col-md-6 offset-1"style="margin-top: 150px;">
    <table class="table">
      <tbody>
        <tr><td>タイプ</td><td><%= @club.club_type %></td></tr>
        <tr><td>名前</td><td><%= @club.club_name %></td></tr>
        <tr>
          <td><%= link_to "一覧に戻る", public_clubs_path, class: "btn btn-outline-success" %></td>
          <%# if @club.user == current_user %>
            <td><%= link_to '編集する', edit_public_club_path(@club.id), class: "btn btn-outline-success" %></td>
            <td><%= link_to "削除", public_club_path(@club), class: "btn btn-outline-success", method: :delete %></td>
          <%# else %>
            <!--<td></td>-->
            <!--<td></td>-->
          <%# end %>
        </tr>
      </tbody>
    </table>
    </div>
    
  </div>
  </div>
</div>