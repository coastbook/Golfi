<div class="container" style="margin-top: 50px;">
  <div class="col-md-8 offset-md-2"> <%#.offset-md-1 は .col-md-9 を 1 カラムぶん右に移動%>
    <div class="shadow p-3 mb-5 bg-white rounded">

 
  <h2 class="p-3 mb-2 bg-dark text-white">コラム一覧</h2>
  <h1 class="text-right">
    <%= link_to "＋　新規追加", new_admin_column_path, class: "btn btn-dark rounded-pill" %>
  </h1>
  
      <p id="notice"></p>
      <table class="table table-hover">
        <tr class="table-secondary">
          <th>コラムID</th>
          <th>コラム名</th>
        </tr>
          <% @columns.each do |column| %>
            <tr class="table-borderless">
              <td><%= column.id %></td>
              <td><%= link_to column.title, admin_column_path(column) %></td>
            </tr>
            <% end %>
      </table>
    </div>
    
<ul class="pagination justify-content-center">
<%= paginate @columns %>
</ul>

  </div>
</div>