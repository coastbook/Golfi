<div class="container" style="margin-top: 50px;">
  <div class="col-md-8 offset-md-2"> <%#.offset-md-1 は .col-md-9 を 1 カラムぶん右に移動%>
    <div class="shadow p-3 mb-5 bg-white rounded">

<%# form_withを使い、情報を取得するモデル、rails routesで調べたURL、メソッドを指定して、'f.text_field'でデータを引っ張ってきてください。%>
<%= form_with model: @user, url: admin_user_path(@user.id), method: :patch do |f| %>
<table class="table table-hover">

      <h2 class="p-3 mb-2 bg-dark text-white">会員情報編集 </h2>
      

      <tbody>
      <tr><td>ID</td><td><%= @user.id %></td></tr>
      <tr><td>Name</td><td><%= f.text_field :name %></td></tr>
      <tr><td>e-mail</td><td><%= f.text_field :email %></td></tr>
    </tbody>
  </table>
  
  <div class="text-center">
   <%= f.submit '変更を保存', class: "btn btn-outline-success w-50" %>
  </div>
  
<% end %>

    </div>
  </div>
</div>

